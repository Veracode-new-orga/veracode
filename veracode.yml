# Lines 1–24: Pipeline Scan Block
veracode_static_scan:
  push:
    trigger: true              # Line 3
    branches_to_run:
      - '*'                    # Line 5
    branches_to_exclude:       # Line 6 — blank
  pull_request:
    trigger: true
    action:
      - opened
      - synchronize
    target_branch:
      - default_branch
  analysis_on_platform: false
  break_build_policy_findings: false
  break_build_invalid_policy: true
  break_build_on_error: false
  error_message: "Veracode static scan faced a problem..."
  policy: 'Veracode Recommended Medium + SCA'
  create_code_scanning_alert: false
  create_issue: false          # Line 23 ✅
  issues:
    trigger: false
    commands:
      - "Veracode Static Scan"

# Lines 27–40: SCA Scan Block
veracode_sca_scan:
  push:
    trigger: true              # Line 29
    branches_to_run:
      - '*'                    # Line 31
    branches_to_exclude:       # Line 32 — blank
  pull_request:
    trigger: true
    action:
      - opened
      - synchronize
    target_branch:
      - default_branch
  create_issue: false          # Line 40 ✅
